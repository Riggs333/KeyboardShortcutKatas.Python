from KeyboardShortcutKatas._.__.looks import my


def so():
    Far.away(___)


def yesterday():
    ___.all(___)


class Now:

    @staticmethod
    def it(cls):
        import looks as though
        though.they_re()


def made(cls):
    cls.once()


class Troubles:

    def seemed(self):
        so()


class Far:

    @staticmethod
    def away(cls):
        Now().it(___)


class ___:

    @staticmethod
    def all(cls):
        my()
